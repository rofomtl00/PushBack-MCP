services:
  - type: web
    name: pushback-mcp
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: python mcp_server.py --http
    envVars:
      - key: MCP_PORT
        value: "10000"
      - key: PORT
        value: "10000"
